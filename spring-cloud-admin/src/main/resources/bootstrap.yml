server:
  port: 8000

spring:
  application:
    name: spring-cloud-admin

eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka/

management:
  endpoint:
    health:
      show-details: always
  endpoints:
    web:
      exposure:
        include: ["health","info"]